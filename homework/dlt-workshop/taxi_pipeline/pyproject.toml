[project]
name = "taxi-pipeline"
version = "0.1.0"
description = "nyc taxi data pipeline powered by dlt and duckdb"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dlt[duckdb,workspace]>=1.22.1",
]
